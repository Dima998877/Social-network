const initialState = {
  friendsData: [
    { id: 1, name: 'Dima' },
    { id: 2, name: 'Ann' },
    { id: 3, name: 'Marie' },
    { id: 4, name: 'Pete' },
    { id: 5, name: 'Mike' },
  ],
};

export const friendsBarReducer = (state = initialState, action) => {
  return state;
};
